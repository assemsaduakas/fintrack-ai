Recording Checklist — FinTrack AI Demo

- [ ] Activate virtual environment: `source venv312/bin/activate`
- [ ] Ensure `OPENAI_API_KEY` is set (tests use a dummy key; for live API use your key).
- [ ] Start screen recorder.
- [ ] Show project layout: `ls -la` and open `main.py`, `database.py`, `tests/`.
- [ ] Run `bash scripts/demo.sh` to show automated example interactions.
- [ ] Run `python -m pytest -q` and show passing tests.
- [ ] Open `fintrack_ai.log` (tail) to show logs during execution.
- [ ] Narrate the delegation, sanitization, and rate-limiting features.
- [ ] End recording and save video file.
